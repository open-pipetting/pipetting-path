'use strict';

var keyMirror = require('react/lib/keyMirror');

module.exports = keyMirror({
  UPDATE_SETTINGS: null,
  UPDATE_SIZE: null,

  // ADD_DEVICE: null,
  // REMOVE_DEVICE: null,

  ADD_LAYER: null,
  REMOVE_LAYER: null,
  UPDATE_LAYER: null,
});
